# V-tach
V-Tach Detection Mid-Semester Project

1. Install requirements:
   pip install -r requirements.txt

2. Run full pipeline:
   python scripts/vtach_pipeline.py

3. Data is automatically downloaded from Kaggle using kagglehub.

4. Outputs:
   - Class distribution plot
   - Waveform plot
   - Baseline model metrics
